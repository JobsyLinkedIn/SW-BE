# SW-BE

# to seed collections type in terminal

node seed.js

# to run the server type in terminal

node server.js

# for code styling

1-npm install --save-dev prettier --legacy-peer-deps (download this from terminal)
2-npx prettier --write . (run this command always before pushing your code to main)

# for linting

# not yet working

1-npm install --save-dev eslint eslint-plugin-prettier eslint-plugin-unicorn --legacy-peer-deps (download this from terminal)

2-npx eslint . --fix (run this command always before pushing your code to main)
